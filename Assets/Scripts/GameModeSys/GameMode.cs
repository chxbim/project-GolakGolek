// GameMode.cs
// Enum shared — dipakai GameSession, GameModeController, GameCashierData, CartSystem-CartPayload.
// Pisah file supaya tidak ada circular dependency.

public enum GameMode
{
    TimeAttack,
    Golek
}